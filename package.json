{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm ci --prefix backend && npm ci --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "api:generate": "tsp compile api/main.tsp --emit @typespec/openapi3 --output-dir api/ --options @typespec/openapi3.file-type=yaml",
    "api:generate:all": "npm run api:generate && cd frontend && npm run generate:types",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@typespec/compiler": "^0.56.0",
    "@typespec/http": "^0.56.0",
    "@typespec/openapi3": "^0.56.0",
    "@typespec/rest": "^0.56.0",
    "concurrently": "^8.2.2"
  }
}